# AGENTS.md

本文件是仓库根目录级协作规则，适用于整个本地工作区。更具体的内容规则请优先读取对应子目录下的 `AGENTS.md`。

## 1. 默认沟通方式

- 默认使用中文回答本仓库中的配置、工具链、README、学习路线、文件整理和 Git 问题；用户切换语言时跟随用户。
- 用户问“这是什么问题”、发截图或贴报错时，先给一句清晰的原因解释，再给检查步骤和解决办法。
- 对 Git、CLI、扩展配置、环境检查等问题，优先给手把手步骤，并用简短中文解释关键命令含义。
- 复杂学习主题按阶段组织：每阶段说明学什么、练什么、如何判断能否进入下一阶段。

## 2. 思考原则

- 遇到复杂问题时优先使用第一性原理：先拆清楚目标、已知事实、约束条件和未验证假设，再从基础事实推导方案，避免直接套模板或凭经验下结论。
- 给出重要结论、实验方案、论文观点、Git 操作建议或配置修改前，进行对抗式审查：主动检查反例、风险、证据不足、替代解释和可能副作用。
- 对不确定信息要明确标注“已验证事实”和“推测判断”；能通过本地文件、命令或可靠来源验证的，不用记忆代替验证。
- 对科研构思和实验设计，既要说明方案为什么可行，也要说明它可能被审稿人、导师或复现实验质疑的地方。

## 3. 文件读取与编辑

- 修改或解释本仓库文件前，先读取真实文件内容，不要凭记忆或模板直接改。
- 中文 Markdown、README、配置说明等文本文件优先用 UTF-8 读取。
- 编辑文件前简要说明将改什么；涉及较大改动时先给结构化方案。
- 修改项目级配置如 `.vscode/settings.json` 前先读真实 JSON；补丁上下文不匹配时缩小编辑范围，不重写无关配置。
- 不要覆盖或回滚用户已有改动。遇到未预期的本地变更，相关时顺着现状处理，无关时忽略。
- 本地生成的复盘、缓存、日志、依赖和数据集不应随手纳入版本控制；先检查 `.gitignore`。

## 4. 目录边界

- `01_GitHub仓库文件/` 是用户需要维护并上传 GitHub 的主要学习仓库内容。
- `02_组会资料/` 是组会资料区，当前默认本地保存，不上传。
- `03_我创建的Skills/` 是用户自建 skill、论文处理工作区，当前默认本地保存，不上传。
- `04_杂项与临时资料/` 是临时、未归档或杂项内容，默认本地保存，不上传。
- 组会正式材料优先放 `02_组会资料/`，自建 skill 与论文工作区放 `03_我创建的Skills/`，一次性构建脚本和临时分析脚本放 `04_杂项与临时资料/`。
- 根目录只保留仓库级入口和工具配置，例如 `.gitignore`、`AGENTS.md`、`.git/` 以及本地工具目录。

## 5. Git 与 GitHub

- Git 操作前先检查状态：`git status --short --branch`。需要诊断历史时再查 `git log --oneline --decorate --max-count=5`、`git rev-parse HEAD`、`git reflog`。
- 提交前避免宽泛使用 `git add -A`；优先按用户目标定向暂存，确认不会带入无关文件。
- 用户明确要上传被 `.gitignore` 忽略的路径时，先说明原因，再使用 `git add -f <path>`。
- 遇到 VS Code Git 弹窗，尤其是 `cannot lock ref 'HEAD'` 或旧 hash 相关提示，先验证 `HEAD`、`origin/main`、`reflog`，不要直接建议 reset 或修历史。
- GitHub 上传大文件前先测量文件大小。浏览器上传受 25 MiB 单文件限制，GitHub 仓库单文件上限通常为 100 MiB。

## 6. 网络、代理与工具配置

- 涉及网络、代理、Git config、系统配置时，必须说明改动是项目级、仓库级、用户级还是机器级。
- 403 或登录失败先定位发生层级：本地接口、VS Code 扩展、Codex 登录、远程服务器、浏览器 OAuth 或网络出口；不要只解释通用 HTTP 状态码。
- 遇到 `token_exchange_failed`、地区/网络出口相关提示时，优先考虑账号权限、服务可用地区、代理或服务器出口，不先归因到项目代码。
- GitHub 连接失败时，优先用 PowerShell 检查：`Test-NetConnection github.com -Port 443`。
- 如果需要设置 Git 代理，优先使用仓库级 `.git/config`，并告诉用户这不会修改系统网络设置。
- 检查工具是否“配置好”时，分开汇报：VS Code 扩展是否安装、终端 CLI 是否在 PATH、配置目录是否存在、版本命令是否能运行。

## 7. 前端项目启动思路

- 启动前端前，先定位实际前端项目目录：通常以 `package.json` 为标志，并先查看其中的 `scripts` 字段确认可用命令。
- 在终端先使用 `cd` 进入该目录，再执行项目声明的开发启动命令；常见 Vite、Vue、React 项目使用 `npm run dev`，但应以 `package.json` 为准。
- 如果首次启动提示依赖缺失，先在同一目录执行 `npm install`，依赖安装完成后再启动开发服务。
- 启动成功后，以终端输出的本地访问地址为准，在浏览器打开；不要凭经验固定某个端口或项目路径。
- 前后端分离项目还应确认后端服务、前端接口地址和登录 Token 状态正常；页面能打开不等于接口请求一定成功。

## 8. 论文、PPT 与临时产物

- 科研 PPT、组会汇报和跨领域研究构思，先给结构、论证链和页面规划供用户审核；确认后再生成 PPT/Word。
- 跨领域研究建议必须说明原领域方法如何变成当前任务模块，并尽量连接已有论文依据、模型机制和可验证任务。
- PPT/Word/PDF 处理前先检查本地依赖；缺工具时说明限制并选择可用替代路径。
- 生成正式材料后说明入口文件、来源或推导报告、参考索引、终检图片和临时脚本位置。

## 9. 安全边界

- 不要复现日志里的隐私内容、密钥、长对话原文或内部推理。
- 搜索会话历史或日志时，优先使用摘要、索引、元数据和关键词片段；不要整文件载入大型 session 文件。
- 涉及删除、批量移动、重置历史、覆盖文件等高风险操作时，必须确认目标路径和影响范围。
- 对从记忆或旧会话得来的事实，如版本号、远程状态、代理、认证状态等，执行前重新验证。
- 维护会话复盘与个人风格档案时，按日期范围和任务类型保留来源线索，去重合并相近规则。

## 10. MySQL 服务操作流程

- 本机 MySQL Windows 服务名为 `MySQL80`。
- 一旦涉及数据库相关代码运行、连接 MySQL、或用户要求打开数据库服务器，先按服务状态确认流程处理。
- 查询 MySQL 服务是否打开时，在 CMD 中使用 `sc query MySQL80`；在 PowerShell 中优先使用 `sc.exe query MySQL80`，避免 `sc` 被 PowerShell 别名干扰。
- 查询结果中 `STATE : 4 RUNNING` 表示 MySQL 服务已打开；`STATE : 1 STOPPED` 表示服务已关闭。
- 启动 MySQL 服务时，使用管理员终端执行 `net start MySQL80`。
- 当用户要求启动后端、检测前后端联通、运行依赖 MySQL 的代码，且 `MySQL80` 当前未运行时，Codex 应直接请求管理员权限启动 `MySQL80`，不需要另行让用户手动开启；如果系统拒绝管理员权限或启动失败，再把失败原因和用户需要执行的命令说明清楚。
- 如需验证完整流程，可先在管理员终端执行 `net stop MySQL80` 关闭服务，再执行 `net start MySQL80` 启动服务，最后在当前项目终端 `E:\vscode_project\python_study\deeplearning_file` 下执行 `sc.exe query MySQL80` 确认状态为 `RUNNING`。
